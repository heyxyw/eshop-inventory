<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mapper.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zhouq.eshop.inventory.mapper.UserMapper">
 
  	<select id="findUserById" resultType="com.zhouq.eshop.inventory.model.User">
		select name,age from user
		WHERE id = #{id}
  	</select>
 
</mapper>